<!-- if users have story -->
<% if(typeof stories == 'object' && stories.length > 0){%>
<p>yes, you do have stories</p>

<div class="row">
  <div class="col-4">
    <div class="list-group" id="list-tab" role="tablist">
      <% stories.forEach(function(val){ %>

      <a
        class="list-group-item list-group-item-action"
        id="list-<%= val.title %>-list"
        data-toggle="list"
        href="#list-<%= val.title %>"
        role="tab"
        aria-controls="<%= val.title %>"
        ><%= val.title %></a
      >
      <% }) %>
    </div>
  </div>
  <div class="col-8">
    <div class="tab-content" id="nav-tabContent">
      <% stories.forEach(function(val){ %>
      <div
        class="tab-pane fade show"
        id="list-<%= val.title %>"
        role="tabpanel"
        aria-labelledby="list-<%= val.title %>-list"
      >
        <p><%= helpers.stripTags(val.body) %></p>
        <a href="/stories/edit/<%= val._id %>" class="btn btn-float">
          <i class="fas fa-edit"></i>
        </a>
        <form action="/stories/<%= val._id %>" method="POST" id="delete-form">
          <input type="hidden" name="_method" value="DELETE" />
          <button type="submit" class="btn red">
            <i class="fas fa-trash"></i>
          </button>
        </form>
      </div>

      <% }) %>
    </div>
  </div>
</div>
<%} else{ %>
<p>you have not created any stories</p>
<% } %>
